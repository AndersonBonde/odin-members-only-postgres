<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %>></title>
  <link rel="stylesheet" href="./stylesheets/style.css">
</head>
<body>
  <main>
    <header>
      <h1><%= title %></h1>
    </header>
    <form action="" method="post">
      <div class="form_row">
        <label for="title">Title</label>
        <input type="text" name="title" value="<%= undefined === locals.message ? '' : message.title %>" required>
      </div>
      <div class="form_row">
        <label for="content">Message</label>
        <textarea name="content" required><%= undefined === locals.message ? '' : message.content %></textarea>
      </div>
      <button type="submit">Submit</button>
    </form>

    <%- include('./partials/errors.ejs') %>
  </main>
</body>
</html>